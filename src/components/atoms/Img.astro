---
import { Image } from "astro:assets"
import type { ImageMetadata } from "astro"

interface Props {
  src: ImageMetadata
  title: string
  className: string
}

const { src, title, className } = Astro.props
---

<Image class=`aspect-auto ${className}` src={src} alt={title} title={title} />
