---
interface Props {
  name: string
  placeholder: string
  className: string
}

const { name, placeholder, className } = Astro.props
---

<input class={className} type="text" name={name} id={name} placeholder={placeholder} />
